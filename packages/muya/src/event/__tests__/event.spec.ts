import { describe, expect, it } from 'vitest';

describe('test template', () => {
    it('should "xxx" function work correctly', () => {
        const raw = [1, 2, 3, 4, 5];

        expect(raw).toEqual([1, 2, 3, 4, 5]);
    });
});
