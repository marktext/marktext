# MarkText 使用指南

## 问题解决方案

### 1. ✅ 已修复：关闭文档时错误提示保存

**问题描述**：之前即使没有修改文档，关闭时也会提示保存。

**解决方案**：已在 `src/renderer/store/editor.js:1169-1195` 修复此问题。

**修复逻辑**：
- 当文件在磁盘被外部程序修改时：
  - 如果编辑器中的文件**已保存**（无本地修改）→ 自动加载外部改动，显示信息提示
  - 如果编辑器中的文件**有未保存改动** → 询问用户是否重新加载

**测试方法**：
1. 打开一个 Markdown 文件
2. 不做任何修改
3. 用外部编辑器（如 VSCode）修改并保存该文件
4. 回到 MarkText，关闭窗口
5. ✅ 现在应该不会提示"是否保存"

---

### 2. ✅ 多窗口功能已存在！

**功能说明**：MarkText 已经支持多窗口显示，可以同时打开多个独立窗口查看不同文档。

**使用方法**：

#### 方法 1：通过菜单
```
File → New Window
```

#### 方法 2：通过快捷键
查看 File 菜单中 "New Window" 后面显示的快捷键（通常是 `Ctrl+Shift+N` 或 `Cmd+Shift+N`）

**功能特点**：
- ✅ 每个窗口可以独立显示不同的文档或标签页
- ✅ 窗口之间互不影响，可以并排放置
- ✅ 智能文件分配：打开新文件时会自动分配到最合适的窗口
- ✅ 可以创建任意多个窗口

**使用场景**：
- 同时查看和编辑多个 Markdown 文件
- 对比两个文档的内容
- 在不同屏幕上显示不同文档

---

### 3. ✅ PDF 导出功能已存在！

**功能说明**：MarkText 已完整实现 PDF 导出功能，并支持丰富的自定义选项。

**使用方法**：

#### 导出为 PDF
```
File → Export → PDF
```

#### 可自定义选项
- **页面设置**：
  - 页面大小（A3, A4, A5, Letter, Tabloid, Custom）
  - 页面方向（纵向/横向）
  - 页边距（上、下、左、右）

- **字体设置**：
  - 字体类型
  - 字体大小
  - 行高

- **文档结构**：
  - 页眉和页脚
  - 自动生成目录
  - 标题样式

- **主题选择**：
  - Academic（学术）
  - GitHub
  - Liber

#### 导出为 HTML
如果需要 HTML 格式，也可以通过以下菜单导出：
```
File → Export → HTML
```

**导出位置**：导出后会弹出保存对话框，选择保存位置即可。

---

## 其他功能提示

### 快捷键
建议查看菜单栏中各功能后面显示的快捷键，可以大幅提升效率。

### 自动保存
在 Preferences（偏好设置）中可以开启自动保存功能：
```
File → Preferences → Auto Save
```

开启后，文件会在修改后自动保存，无需手动保存。

---

## 代码修改说明

### 修复的文件
- `src/renderer/store/editor.js` (line 1169-1195)

### 修改内容
改进了文件监听器的行为，区分"外部文件改变"和"本地未保存改动"两种情况，避免在没有本地改动时错误地提示保存。

### 技术细节
```javascript
// 修复前：无条件设置 isSaved = false
commit('SET_SAVE_STATUS_BY_TAB', { tab, status: false })

// 修复后：根据当前保存状态决定行为
if (isSaved) {
  // 已保存 → 自动加载外部改动
  commit('LOAD_CHANGE', change)
} else {
  // 有未保存改动 → 询问用户
  commit('SET_SAVE_STATUS_BY_TAB', { tab, status: false })
}
```

---

## 总结

✅ **问题 1**：已修复
✅ **问题 2**：功能已存在（File → New Window）
✅ **问题 3**：功能已存在（File → Export → PDF）

所有功能均可正常使用！
